import React, { Component } from 'react';

export default class AddCard extends Component {
  static propTypes = {

  };

  render() {
    return (
      <footer className="card-groups-add-card">Add a card...</footer>
    );
  }
}
