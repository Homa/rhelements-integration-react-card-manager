import React, { Component } from 'react';

export default class AddGroup extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div className="card-groups-add-group">
        Add a new column...
      </div>
    );
  }
}
